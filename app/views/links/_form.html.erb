<%= form_with(model: link) do |form| %>
  <% if link.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
        <% link.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :input_url, style: "display: block" %>
    <%= form.text_field :input_url %>
  </div>

  <div>
    <%= form.label :shrinked_url, style: "display: block" %>
    <%= form.text_field :shrinked_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
